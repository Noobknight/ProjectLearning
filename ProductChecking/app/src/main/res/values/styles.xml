<resources>

    <!-- Base application theme. -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light.NoActionBar" />

    <style name="AppTheme" parent="AppBaseTheme">

        <!--<item name="android:textColorPrimary">@android:color/white</item>-->
        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>

        <item name="android:windowContentOverlay">@null</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <!--Toolbar Theme / Apply white arrow -->
        <item name="colorControlNormal">@android:color/white</item>
        <item name="actionBarTheme">@style/AppTheme.ActionBarTheme</item>
        <item name="android:textColorPrimary">@android:color/white</item>

        <item name="android:windowBackground">@android:color/white</item>

        <!-- Material Theme -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <style name="AppTheme.ActionBarTheme" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <!-- White arrow -->
        <item name="android:background">@color/colorPrimary</item>
        <item name="colorControlNormal">@android:color/white</item>
    </style>

    <style name="DrawerArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@color/drawerArrowColor</item>
    </style>

    <style name="NavigationViewTheme" parent="AppBaseTheme">
        <item name="textAppearanceListItem">@style/TextAppearance.AppCompat.Body2</item>
        <item name="itemTextColor">@color/material_black</item>
        <item name="itemIconTint">@color/material_black</item>
    </style>

    <style name="ThemeSeekbar">
        <item name="colorAccent">#FFFF00</item>
    </style>

    <style name="TypeProgress" parent="Widget.AppCompat.ProgressBar.Horizontal" />

    <style name="ProgressBarStyle">
        <item name="colorAccent">@color/material_pink_500</item>
    </style>

    <style name="CardViewStyle" parent="android:Widget.TextView">
        <item name="android:foreground">@drawable/selector_bg_card</item>
        <item name="cardCornerRadius">@dimen/card_corner_radius</item>
        <item name="cardElevation">@dimen/card_elevation</item>
        <item name="cardPreventCornerOverlap">false</item>
        <item name="cardUseCompatPadding">true</item>
    </style>


    <style name="AppCompatAlertDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorAccent">@color/material_pink_500</item>
        <item name="android:textColorPrimary">@color/material_grey_500</item>
        <item name="android:background">@color/material_white</item>
    </style>


</resources>
